import React from 'react'

export default function Basket({ принимаем базу данных с списком заказов пользователя}) {
  return (
    <section>
        <h1>basket</h1>
        <main>
        {/* BAZA.map((element) => {  
        return (
            <div>
            element
            </div>
            )
         }); */}
        </main>
    </section>
  )
}
